import { useState } from 'react'
export function ContentEditor({ copyData, onSave }: { copyData: Record<string, unknown>; onSave: (copy: Record<string, unknown>) => void }) { const [value, setValue] = useState(JSON.stringify(copyData, null, 2)); const save = () => { try { onSave(JSON.parse(value) as Record<string, unknown>) } catch { /* input remains visible for correction */ } }; return <div className="space-y-2"><textarea value={value} onChange={(event) => setValue(event.target.value)} rows={12} className="w-full rounded border border-ui-border bg-ui-bg p-3 font-mono text-xs text-ui-text" /><button className="rounded bg-brand-red px-3 py-2 text-sm text-white" onClick={save}>Salvar nova versão</button></div> }
