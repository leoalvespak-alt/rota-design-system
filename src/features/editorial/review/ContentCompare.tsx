export function ContentCompare({ before, after }: { before: Record<string, unknown>; after: Record<string, unknown> }) { return <div className="grid gap-3 md:grid-cols-2"><pre className="overflow-auto rounded border border-ui-border bg-ui-panel p-3 text-xs text-ui-muted">{JSON.stringify(before, null, 2)}</pre><pre className="overflow-auto rounded border border-brand-red/40 bg-ui-panel p-3 text-xs text-ui-muted">{JSON.stringify(after, null, 2)}</pre></div> }
