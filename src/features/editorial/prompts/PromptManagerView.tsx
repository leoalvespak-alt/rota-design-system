import { useEffect, useState } from 'react'
import { apiFetch } from '@/lib/api/client'
type Prompt = { id: string; name: string; type: string; version: number; active: boolean }
export function PromptManagerView() { const [items, setItems] = useState<Prompt[]>([]); useEffect(() => { apiFetch<Prompt[]>('/api/prompts').then(setItems).catch(() => undefined) }, []); return <section className="mx-auto max-w-5xl"><h1 className="font-heading text-2xl font-bold text-ui-text">Prompts</h1><p className="mt-1 text-sm text-ui-muted">Templates versionados que guiam cada etapa do motor.</p><div className="mt-5 space-y-2">{items.map((item) => <div key={item.id} className="flex items-center justify-between rounded-lg border border-ui-border bg-ui-panel p-4"><div><strong className="text-ui-text">{item.name}</strong><p className="text-sm text-ui-muted">{item.type} · versão {item.version}</p></div><span className="text-xs text-ui-muted">{item.active ? 'ativo' : 'inativo'}</span></div>)}</div></section> }
