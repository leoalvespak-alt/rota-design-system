import { apiFetch } from '@/lib/api/client'
export function PlanReviewView({ plan, onBack }: { plan: { id: string; title: string; items: Array<{ id?: string; scheduledDate?: string; format: string; hookStrategy?: string; status?: string }> }; onBack: () => void }) { const approve = async () => { await apiFetch(`/api/plans/${plan.id}/approve`, { method: 'PATCH' }); onBack() }; return <section className="mx-auto max-w-6xl space-y-4"><div className="flex justify-between"><div><button className="text-sm text-ui-muted" onClick={onBack}>← Voltar</button><h1 className="mt-1 font-heading text-2xl font-bold text-ui-text">{plan.title}</h1></div><button className="rounded bg-brand-red px-4 py-2 text-sm text-white" onClick={() => void approve()}>Aprovar plano</button></div><div className="overflow-x-auto rounded-lg border border-ui-border"><table className="min-w-full text-left text-sm"><thead className="bg-ui-panel2 text-ui-muted"><tr><th className="px-3 py-2">Data</th><th className="px-3 py-2">Formato</th><th className="px-3 py-2">Hook</th><th className="px-3 py-2">Status</th></tr></thead><tbody>{plan.items.map((item, index) => <tr key={item.id ?? index} className="border-t border-ui-border"><td className="px-3 py-2 text-ui-text">{item.scheduledDate}</td><td className="px-3 py-2 text-ui-text">{item.format}</td><td className="px-3 py-2 text-ui-muted">{item.hookStrategy}</td><td className="px-3 py-2 text-ui-muted">{item.status ?? 'planned'}</td></tr>)}</tbody></table></div></section> }
